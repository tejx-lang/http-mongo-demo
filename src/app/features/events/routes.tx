// Route adapter for the events feature.
import { ServerResponse } from "../../../modules/server/index.tx";
import { AppState } from "../../core/state.tx";
import { handleEvents } from "./handler.tx";

export function handleEventsRoute(method: string, state: AppState): ServerResponse {
    return handleEvents(method, state);
}

// Public auth module entrypoint.
import { ServerResponse } from "../../../modules/server/index.tx";
import { AppState } from "../../core/state.tx";
import { handleLogin } from "./handler.tx";

export function handleAuthRoute(method: string, body: string, state: AppState): ServerResponse {
    return handleLogin(method, body, state);
}
